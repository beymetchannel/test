name: Manual Scraper Test (FINAL SIMPLE FIX)

on:
  workflow_dispatch:

jobs:
  test-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 1. Python環境のセットアップ
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 2. インストールと実行を一つのステップで強制（最もエラーが出にくい）
      - name: Install dependencies and Run Scraper
        run: |
          pip install selenium webdriver-manager
          python scraper.py
